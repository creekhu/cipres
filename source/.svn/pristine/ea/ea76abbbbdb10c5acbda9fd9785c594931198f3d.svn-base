package org.ngbw.sdk.api.data;


/**
 * @author Roland H. Niedner <br />
 * 
 * The SimpleQuery extends the DataResourceQuery and defines all
 * functionality to query 1 Dataset for a single search phrase.
 * It is called from the SimpleSearchMetaQuery when it delegates
 * the search request from the sdk client to all requested Datasets.
 *
 */
public interface SimpleQuery extends DataResourceQuery {
	//consciously left empty
	//serves currently only to demarcate the query type.
	//add methods if required in future versions
}
