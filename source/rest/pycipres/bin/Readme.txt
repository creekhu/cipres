Can use tooltest to submit jobs:
	tooltest <dir> run|validate

Can use runtemplate to dump full job status:
	runtemplate -j <jobhandle> 


Runs as pycipres user, a direct end user account which
can be created by running testAndDemoAccounts
